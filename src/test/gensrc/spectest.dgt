// GEN_NOTICE
// Timestamp: TIME_STAMP

// tests for substitutions

line without substitution

line with COUNT #(SUBSTITUTION)s
line ending with SUBSTITUTION
SUBSTITUTION at the beginning

// tests for the token concat operator

## at start of the line
line ending with a ##
line with ## in the middle

// tests for the preprocessor directives

#define(_NESTED){#name(NESTED)}
#if (ENABLED)
{
	#delete(ENABLED)
	ENABLED set to true
	#if (NESTED){and _NESTED set to true}#else{but _NESTED set to false}
}
#else
{
	#define(_ENABLED){#name(ENABLED)}
	_ENABLED set to ENABLED
	#if (NESTED){but _NESTED set to true}#else{and _NESTED set to false}
}

#for("./spectest-Test.dgs", "./spectest-Base.dgs")
{
	- the value of #name(COUNT) in SPEC_NAME is COUNT
}

#for(INDEX <- 1 .. 10)
{
	INDEX
}

------ spectest-Base.dgs ------
#include("./spectest-Base.dgs")
-------------------------------
