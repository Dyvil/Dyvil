package dyvil.test

import java.util.{ Arrays, Collections, Map, HashMap }
using dyvil.maps.MapUtils._

public class Main
{
	public static void main([String] args)
	{
		Map[String, String] m = Map("a" -> "1",
									"b" -> "2",
									null -> "null")
									
		System out println if (m ? "a") "A" else "null"
		m += ("c", "3")
		m -= "b"
		System out println(m @ null)
		System out println m
	}
}
