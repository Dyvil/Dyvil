package dyvil.test

import java.util.{ Arrays, Collections, Map, HashMap, List }
using dyvil.maps.MapUtils._

public class Main
{
	public static void main([String] args)
	{
		printLength "hello world"
	}
	
	public static void printLength[T >= CharSequence](T t) = System out println(t length);
}
