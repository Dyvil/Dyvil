package dyvil.test

import java.util.{ Arrays, Collections, Map, List, ArrayList, Set }
import java.util.function.{ Supplier, Function }
import java.util.stream.Collectors;
using dyvil.maps.MapUtils._
using dyvil.collections.ListUtils._

public class Main
{
	public static void main([String] args)
	{
		List[String] list = List("a", "b", "c", "d")
		list = list stream map(s => s toUpperCase) collect(Collectors toList)
		System out println list
	}
}
