package dyvil.test

import java.util.{ Arrays, Collections, Map, HashMap, List, ArrayList }
using dyvil.maps.MapUtils._
using dyvil.collections.ListUtils._

public class Main
{
	public static void main([String] args)
	{
		List[String] list = List("a", "b", "c")
		list += 1
		String s = get(list)
		System out println s
		System out println list
	}
	
	public static T get[T]([T] array) = array(0);
	
	public static T get[T](List[T] list) = list.get(0);
}
