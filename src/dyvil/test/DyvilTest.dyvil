package dyvil.test;

// Import a single class
import java.util.Random;
// Import everything from that package.
import java.lang.;
// Import a static field
import java.test.Test.var1;
// Import a static method
import java.test.Test.setVar1(int);

// Line comment
/* Block comment
 * public - Everyone can use the member
 * private - Only this class / instance can use the member
 * inherited - This class and subclasses can use the member
 */

// Usual class        [Generics]
public class DyvilTest[?, ? extends Object]
{
    public int var1 = 3;
    public float var2 = 4.0;
    public float double = 4.000000000000000000000001;
    public String name = "DyvilTest";
    public int<> intarray = {};
    public float<> array = { 1, 2, 3 } + {4.5, 5, 6.2}; // Returns {1, 2, 3, 4, 5, 6}
    
    public DyvilTest()
    {
    }
    
    public DyvilTest(int var1, float var2)
    {
        this.var1 = var1;
        this.var2 = var2;
        
        intarray<0> = var1;
    }
    
    public {int, int} getVars()
    {
        return {var1, var2}
    }
    
    public {int, float} getVars2(int i)
    {
        return {var1, var2 + i};
    }
    
    public float opDouble(float f, func op)
    {
        return double.(func)(f);
    }
    
    public property name
    {
        get:              { return this.name; }
        set(String name): { this.name = name; }
    }
    
    public void printName(PrintStream stream)
    {
        stream.println(this.name);
    }
}

// Singleton
public object InstanceTest
{
}

// Enum
public enum EnumTest
{
}

// Interface
public interface InterfaceTest
{
}