package dyvil.tools.dpf.model

import dyvil.tools.parsing.Name
import dyvil.tools.dpf.model.value.Value

public interface Property extends NodeElement
{
	override func name() -> Name

	func value() -> Value
}
