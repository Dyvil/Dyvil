package dyvil.lang

interface Formattable
{
	@BytecodeName("toString")
	static extension func apply(this: String, from: Formattable) -> String
	{
		let buffer = new StringBuilder
		from.toString(buffer)
		return buffer.toString
	}

	func toString(buffer: StringBuilder) -> void
}
