// GEN_NOTICE
// Timestamp: TIME_STAMP

package dyvil.ref.unboxed

import dyvil.ref.TYPE_NAME##Ref
import dyvil.ref.ObjectRef

class Unboxed##TYPE_NAME##Ref(
	protected let ref: ObjectRef<WRAPPER>
) implements TYPE_NAME##Ref
{
	override func get() -> PRIMITIVE = this.ref.get

	override func set(value: PRIMITIVE) -> void = this.ref.set(value)
}
