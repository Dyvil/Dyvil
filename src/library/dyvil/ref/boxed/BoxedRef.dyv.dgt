// GEN_NOTICE
// Timestamp: TIME_STAMP

package dyvil.ref.boxed

import dyvil.ref.TYPE_NAME##Ref
import dyvil.ref.ObjectRef

public class Boxed##TYPE_NAME##Ref(
	protected let ref: TYPE_NAME##Ref
) implements ObjectRef<WRAPPER>
{
	override func get() -> WRAPPER = this.ref.get

	override func set(WRAPPER value) -> void = this.ref.set(value)
}
