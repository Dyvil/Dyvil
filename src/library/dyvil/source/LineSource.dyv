package dyvil.source

case class LineSource(private let line: String) implements Source
{
	override var lineCount: int { @BytecodeName("lineCount") get: 1 }

	override var text: String { @BytecodeName("text") get: this.line }

	override func line(at index: int) -> String! = index == 1 ? this.line : null
}
